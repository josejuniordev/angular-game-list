class Game {
  id;
  name;
  popularity;
  logo;

  constructor(id, name, popularity, logo) {
    this.id = id;
    this.name = name;
    this.popularity = popularity;
    this.logo = logo;
  }
}

export default Game;
